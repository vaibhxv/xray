# Core (required)
# NOTE: versions below are chosen to provide prebuilt wheels for Python 3.13
# (cp313) on aarch64 (Raspberry Pi OS 64-bit), so nothing compiles from source.
aiohttp==3.11.11
asyncpg==0.30.0
beautifulsoup4==4.12.3
lxml==5.3.0
trafilatura==1.12.1
python-dotenv==1.0.1
Pillow==11.1.0
imagehash==4.3.1

# Image / PDF processing
opencv-python-headless==4.11.0.86
PyMuPDF==1.25.5
pdfplumber==0.11.4

# OCR (Tesseract binary must be installed via apt: `sudo apt install tesseract-ocr`)
pytesseract==0.3.13

# ---- Optional / heavy (uncomment to enable) --------------------------------
# JavaScript-rendered pages. After install run: playwright install chromium
# playwright==1.47.0
#
# Local LLM metadata extraction (Gemma GGUF via llama.cpp).
# llama-cpp-python==0.2.90
#
# Heavier OCR engine (better accuracy, large download):
# easyocr==1.7.2
